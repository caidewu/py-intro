---
layout: two-cols
---

`s` 说明符用于字符串

点击 [这里](https://pyformat.info/#number) 了解更多关于格式说明符的详细信息，

或查看旧风格与新风格字符串插值的对比。

<v-click>

**不同的字符串插值方式**

<span v-mark.highlight.red="1">  各有优缺点。 </span>

</v-click>

<v-click>

通常选择使用哪种方式

是<span v-mark.underline.greeen="2"> 个人偏好的问题。</span>

在本课程中，示例和参考答案将主要使用 `f-字符串` 和 `format 方法`。

</v-click>


::right::

```py {monaco-run} {height:'120px'}
print("%s concatenated with %s produces %s" % ("water", "melon", "water"+"melon"))
print("{0} concatenated with {1} produces {0}{1}".format("water", "melon"))
print(f"{'water'} concatenated with {'melon'} produces {'water' + 'melon'}")
```
